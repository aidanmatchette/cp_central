import { createTheme } from "@mui/material/styles";

const theme = createTheme  ({
    palette: {
      type: 'light',
      primary: {
        main: '#118888',
      },
      secondary: {
        main: '#ff5500',
      },
    },
  });
export default theme

